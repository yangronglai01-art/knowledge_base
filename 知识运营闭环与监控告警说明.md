# 知识运营闭环与监控告警说明（Gap #3 + #4）

本文档说明 RAG 助手「知犀」补短板中的**知识运营闭环**（用户反馈 + 知识版本化）与**监控告警**（查询指标 + 轻量告警）两部分能力。

---

## 一、总体架构

```
用户查询 ──► 查询工作流(KBQueryWorkflow)
                │
                ├─ node_search_embedding ──► 记录检索延迟 / 召回数
                ├─ ...（RRF / 重排序）
                └─ node_answer_output ─────► 记录生成延迟 / 答案长度 / 无结果
                     │
                     ▼
         MongoDB: query_metrics（查询指标）
                     │
                     ▼
              聚合统计 + 轻量告警规则 ──► 告警日志 / 查询接口

用户反馈(点赞/点踩/纠错) ──► MongoDB: chat_feedback ──► 知识优化依据

文档导入 ──► node_import_milvus ──► MongoDB: document_versions（版本 + 内容指纹）
```

---

## 二、知识运营闭环（Gap #3）

### 2.1 用户反馈

用户可对助手回答进行**点赞 / 点踩 / 纠错**，反馈沉淀到 `chat_feedback` 集合，作为知识内容优化的依据（点踩、纠错高频的知识点优先回填修正）。

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/feedback` | POST | 提交反馈 |
| `/feedback/stats` | GET | 反馈聚合统计（可选 `?session_id=` 过滤） |

**提交反馈示例：**

```json
POST /feedback
{
  "session_id": "s-xxx",
  "message_id": "m-xxx",
  "question": "BSL-75A空压机高温报警怎么处理？",
  "answer": "……",
  "feedback_type": "correction",
  "correction": "还应检查温控阀回油是否经过冷却器",
  "user_id": "u-xxx"
}
```

反馈类型：`like`（点赞）、`dislike`（点踩）、`correction`（纠错，`correction` 字段携带正确答案）。

实现：`knowledge_base/utils/feedback_utils.py`

### 2.2 知识版本化

文档每次成功导入，`node_import_milvus` 会向 `document_versions` 集合写入一条版本记录，支撑知识回滚与审计：

| 字段 | 说明 |
| --- | --- |
| `file_title` | 源文档标题 |
| `version` | 自增版本号（同一文档每次导入 +1） |
| `content_hash` | 内容指纹（切片正文拼接的 MD5），用于识别内容是否变化 |
| `chunk_count` | 切片数量 |
| `dept` / `clearance_level` | 导入时的权限元数据 |
| `imported_at` | 导入时间戳 |

实现：`knowledge_base/utils/version_utils.py`（`save_document_version` / `get_latest_version` / `list_versions`）

---

## 三、监控告警（Gap #4）

### 3.1 查询指标

查询链路各节点按 `task_id` 幂等 upsert 指标到 `query_metrics` 集合，形成完整查询监控记录：

| 字段 | 来源 | 说明 |
| --- | --- | --- |
| `retrieval_latency_ms` | node_search_embedding | 检索延迟 |
| `retrieval_count` | node_search_embedding | 检索召回切片数 |
| `generation_latency_ms` | node_answer_output | 答案生成延迟 |
| `rerank_count` | node_answer_output | 重排序后保留切片数 |
| `answer_length` | node_answer_output | 答案长度 |
| `no_result` | node_answer_output | 是否无结果（答案为空） |
| `total_latency_ms` | query_service | 整条查询总延迟 |
| `durations` | query_service | 各节点耗时明细 |
| `status` / `error` | query_service | 完成状态 / 失败原因 |

实现：`knowledge_base/utils/metrics_utils.py`

### 3.2 轻量告警规则

基于最近时间窗口（默认 300s）的聚合指标触发告警，触发即写告警日志：

| 规则 | 阈值 | 级别 |
| --- | --- | --- |
| 无结果率过高 | > 30% | warning |
| 查询失败率过高 | > 20% | critical |
| 平均检索延迟过高 | > 3000ms | warning |
| 平均生成延迟过高 | > 30000ms | warning |

阈值在 `metrics_utils.py` 顶部常量中定义，可按需调整。

### 3.3 监控接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/metrics/summary` | GET | 聚合统计（无结果率、失败率、平均延迟） |
| `/metrics/alerts` | GET | 当前触发的告警列表 |

均支持 `?window_seconds=` 参数（默认 300 秒）。

---

## 四、MongoDB 集合清单

| 集合 | 用途 |
| --- | --- |
| `chat_feedback` | 用户反馈（点赞/点踩/纠错） |
| `document_versions` | 知识版本化记录 |
| `query_metrics` | 查询监控指标 |

---

## 五、相关文件

| 文件 | 说明 |
| --- | --- |
| `knowledge_base/utils/feedback_utils.py` | 反馈存储 |
| `knowledge_base/utils/version_utils.py` | 知识版本化 |
| `knowledge_base/utils/metrics_utils.py` | 指标 + 告警规则 |
| `knowledge_base/web/api/query_service.py` | 反馈 / 监控接口、整条查询指标记录 |
| `knowledge_base/import_process/nodes/node_import_milvus.py` | 导入时记录文档版本 |
| `knowledge_base/query_process/nodes/node_search_embedding.py` | 检索指标埋点 |
| `knowledge_base/query_process/nodes/node_answer_output.py` | 生成指标埋点 |
