# 部署指南

本文档说明如何将「掌柜智库」RAG 助手（导入服务 + 查询服务）及其依赖基础设施部署到 Docker 环境。

---

## 一、架构概览

```
浏览器 / 调用方
   │
   ├── :8000  import-service（文档导入服务，FastAPI）
   │             └─ 上传 → MinIO + 本地暂存 → 解析/切片 → Milvus（记录版本到 Mongo）
   │
   └── :8001  query-service（查询服务，FastAPI）
                 └─ 检索(Milvus) + 历史(Mongo) + LLM → SSE 流式返回答案（指标到 Mongo）

基础设施（docker-compose）：etcd / minio / standalone(Milvus) / attu / mongo
（可选）：langfuse-postgres / langfuse-server
```

两个应用服务共用同一个镜像，通过 compose 的 `command` 区分启动入口。

---

## 二、前置准备

### 1. 安装 Docker 与 Docker Compose
```bash
docker --version
docker compose version
```

### 2. 下载 BGE-M3 嵌入模型（约 2.2GB，挂载方案）

模型不打进镜像，挂载到 `./models/bge-m3`：

```bash
# 方式一：HuggingFace
pip install -U "huggingface_hub[cli]"
huggingface-cli download BAAI/bge-m3 --local-dir models/bge-m3

# 方式二：ModelScope（国内网络更快）
pip install -U modelscope
modelscope download --model BAAI/bge-m3 --local_dir models/bge-m3
```

下载完成后 `models/bge-m3` 目录下应包含 `config.json`、`model.safetensors` 等文件。

---

## 三、配置 `.env`

```bash
cp .env.example .env
# 编辑 .env，把占位符替换为真实值
```

### 必填项（影响能否启动/调用）

| 变量 | 说明 |
| --- | --- |
| `OPENAI_API_KEY` | 大模型 API Key（DashScope 等 OpenAI 兼容服务） |
| `OPENAI_BASE_URL` | 大模型服务地址 |
| `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY` | MinIO 根账号，**与 MinIO 容器共享**（compose 中 MinIO 用它初始化） |
| `LANGFUSE_*` 系列 | Langfuse 观测所需，`LANGFUSE_ENCRYPTION_KEY` 须为 64 位十六进制 |

> ⚠️ `docker compose` 会在解析整个文件时校验 `${VAR:?...}` 必填变量，缺失会直接报错拒绝启动。**若不需要 Langfuse**，可注释掉 `docker-compose.yml` 中的 `langfuse-postgres` 与 `langfuse-server` 两个服务，就不必填 `LANGFUSE_*`。

### 地址类变量说明

`MINIO_ENDPOINT` / `MILVUS_URL` / `MONGO_URL` / `DATA_BASED_ROOT_DIR` / `BGE_M3_PATH` 在容器内**会被 compose 自动覆盖**为内部服务名（`minio:9000`、`http://standalone:19530`、`mongodb://mongo:27017`、`/app/data`、`/models/bge-m3`），`.env` 里的 localhost 值仅用于本地直跑，无需改动。

### 权限过滤开关

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `PERMISSION_FILTER_ENABLED` | true | 权限过滤总开关 |
| `DEFAULT_DOC_DEPT` | `*` | 导入未指定部门时的默认部门（`*`=全员可见） |
| `DEFAULT_DOC_CLEARANCE_LEVEL` | 1 | 导入未指定密级时的默认密级（1-5） |

---

## 四、构建与启动

```bash
# 1. 构建应用镜像（首次较久，因 pyproject 从 pytorch-cuda 拉取 torch，见"常见问题"）
docker compose build import-service query-service

# 2. 启动基础设施 + 应用
docker compose up -d etcd minio standalone mongo import-service query-service

# 3. 查看状态与日志
docker compose ps
docker compose logs -f query-service
```

启动后健康检查：
```bash
curl http://localhost:8000/health   # 导入服务
curl http://localhost:8001/health   # 查询服务
```

> Milvus 首次启动需初始化，健康检查通过前 `import-service`/`query-service` 会因 `depends_on` 等待。

---

## 五、数据初始化

### 1. （可选）迁移旧集合

若升级前 Milvus 里已有旧的 `kb_chunks` 集合（无权限字段），需删除让其按新 schema 重建：

```bash
docker compose exec -it import-service \
  uv run --frozen --no-sync python scripts/maintenance/migrate_add_permission_fields.py
# 提示输入 y 确认删除旧集合
```

> 删除后需重新导入文档；全新建库可跳过此步。

### 2. 导入文档

`/upload` 接收 `file` + `dept` + `clearance_level` 三个表单字段。以一篇售后故障案例为例：

```bash
curl -X POST http://localhost:8000/upload \
  -F "file=@data/documents/售后故障案例/售后故障案例-BSL系列螺杆空压机高温报警处理.md" \
  -F "dept=售后部" \
  -F "clearance_level=2"
```

其余 17 篇按 `data/documents/manifest.csv` 中每篇的 `dept` / `clearance_level` 依此导入。批量导入示例（PowerShell）：

```powershell
# 依据 manifest.csv 的标题->部门/密级映射，逐篇上传（此处给出关键字段示例）
# 实际部门/密级请以 manifest.csv 为准：
#   售后故障案例 -> 售后部/2；产品技术手册 -> 技术部 或 */1；工艺规范 -> 生产部/3~4
#   质量制度 -> 质量部/2~3；设备操作维护 -> 设备部/2 或 生产部/3；业务流程 -> 对应部门/1
Get-ChildItem -Recurse data\documents -Filter *.md | ForEach-Object {
  curl.exe -X POST http://localhost:8000/upload `
    -F "file=@$($_.FullName)" `
    -F "dept=<该篇对应部门>" `
    -F "clearance_level=<该篇对应密级>"
}
```

导入过程中会：写入 Milvus（含 dept/clearance_level 权限字段）→ 在 Mongo `document_versions` 记录版本 + 内容指纹。

---

## 六、端到端验证

### 1. 权限过滤验证（越权访问应被拦截）

```bash
# 生产部、密级2 的用户询问密级4的《螺杆转子磨削工艺要求》——应检索不到该文档
curl -X POST http://localhost:8001/query \
  -H "Content-Type: application/json" \
  -d '{"query":"螺杆转子磨削的磨削余量是多少？","session_id":"demo-1","departments":["生产部"],"clearance_level":2}'
# 返回 {"task_id":"..."}

curl -N http://localhost:8001/stream/<task_id>   # SSE 流式查看结果

# 生产部、密级4 的用户再问同一问题——应能命中
curl -X POST http://localhost:8001/query \
  -H "Content-Type: application/json" \
  -d '{"query":"螺杆转子磨削的磨削余量是多少？","session_id":"demo-2","departments":["生产部"],"clearance_level":4}'
```

### 2. 跑评测（容器内）

```bash
docker compose exec import-service \
  uv run --frozen --no-sync python tests/evaluation/recall_at_k.py --k 5
docker compose exec import-service \
  uv run --frozen --no-sync python tests/evaluation/generation_quality.py --k 5
```

### 3. 监控与告警

```bash
curl "http://localhost:8001/metrics/summary"      # 无结果率/失败率/平均延迟
curl "http://localhost:8001/metrics/alerts"       # 当前触发告警
curl -X POST http://localhost:8001/feedback \
  -H "Content-Type: application/json" \
  -d '{"session_id":"demo-1","feedback_type":"like","question":"...","answer":"..."}'
```

---

## 七、常见问题

### 1. 镜像体积大 / 构建慢

`pyproject.toml` 通过 `[tool.uv.sources]` 强制从 pytorch-cuda(cu128) 安装 torch，镜像约 6-8GB。纯 CPU 部署可瘦身：把 `[tool.uv.sources]` 中 torch/torchvision/torchaudio 的 CUDA 源改为 CPU 源（或移除该段、用 `UV_INDEX_URL` 指向 CPU 源），并确认 `BGE_DEVICE=cpu`。这属于可选优化，不影响功能。

### 2. 模型未挂载报错

若 `models/bge-m3` 目录为空即启动，`BGEM3EmbeddingFunction` 加载 `/models/bge-m3` 会失败。请先按「二、前置准备」下载模型。

### 3. 只跑核心 RAG，不想要 Langfuse / Attu

```bash
docker compose up -d etcd minio standalone mongo import-service query-service
```
仍需在 `.env` 填 `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`；若 `.env` 里 Langfuse 必填项缺失会导致 compose 解析失败，此时注释掉 `langfuse-postgres` / `langfuse-server` 两个服务即可。

### 4. 端口冲突

`8000`（导入）/ `8001`（查询）被占用时，修改 `docker-compose.yml` 中对应服务的 `ports` 左值（宿主机端口）即可。
