# 文档权限过滤说明

为满足规划文档「RAG 必须执行文档权限过滤」的要求，在现有知识库上新增了轻量级的文档级权限控制。

## 权限模型（轻量，预留企业 SSO 接口）

- 文档（导入时）携带两个权限字段：
  - `dept`（部门）：文档所属部门；取 `*` 表示全员可访问。
  - `clearance_level`（密级）：整数 1-5，数值越大越机密。
- 查询用户携带：
  - `departments`：用户所属部门列表。
  - `clearance_level`：用户密级，可访问密级 <= 自身密级 的文档。

> 说明：当前为轻量模型，未来对接企业 SSO/AD 时，只需替换用户身份的来源即可，权限过滤链路不变。

## 过滤规则

文档 `dept` 在用户部门列表内（或为 `*`），且文档 `clearance_level` <= 用户密级。

Milvus filter 示例（用户部门=["销售部","技术部"]，密级=3）：

```
(dept in ["销售部", "技术部"] or dept == "*") and clearance_level <= 3
```

## 涉及改动

| 模块 | 文件 | 说明 |
| --- | --- | --- |
| 权限工具 | `knowledge_base/utils/permission_utils.py` | 统一拼装权限 filter |
| 导入链路 | `import_service.py`、`import_process/state.py`、`node_import_milvus.py` | 上传接收 `dept`/`clearance_level`，写入 Milvus chunks 集合 |
| 查询链路 | `query_service.py`、`query_process/state.py`、`node_search_embedding*.py` | `/query` 接收用户身份，检索时合并权限 filter |
| 单元测试 | `tests/unit/test_permission_utils.py` | filter 拼接逻辑 |

## 接口字段

- 上传 `/upload`（表单字段，均可选）：
  - `dept`：文档所属部门（默认 `*` 全员可访问）
  - `clearance_level`：文档密级（默认 `1`）
- 查询 `/query`（请求体字段，均可选）：
  - `departments`：用户所属部门列表
  - `clearance_level`：用户密级

## 存量集合迁移

旧集合缺少 `dept`/`clearance_level` 字段，需迁移：

```
python scripts/maintenance/migrate_add_permission_fields.py
```

该脚本删除旧 chunks 集合，下次导入时按新 schema 自动重建。
